Setting "Fixed:" to TRUE or attaching a procedure enables the option to specify a model group that the default head movement animation will be applied to.
Make sure you write the exact name of the group, otherwise this will crash your game when spawning the entity.

You can use a procedure to toggle when the head tracking is enabled / disabled at runtime. 